{% extends 'admin/base.html.twig' %}

{% block title %}Editer les horaires
{% endblock %}

{% block body %}
	<main data-spy="scroll" data-target="#navbar" data-offset="0">
		<h1 class="mt-5 pt-5 text-center">Editer les horaires</h1>
		<section>
			<div class="container my-5">
				{{form_start(form)}}
					{{ form_widget(form, { 'attr': {'class': 'form-row'} }) }}
				<button type="submit" class="btn btn-primary">Confirmer</button>
				{{form_end(form)}}
			</div>
		</section>
	</main>
{% endblock %}
