<div id="content{{item.id}}" class="col-sm-10 col-lg-6 col-xl-5 order-2 order-xl-2 my-5 align-self-center ">
	<h3>{{item.title}}</h3>
	{{item.content|raw}}
	<button class="btn btn-info btn-edit" type="button" data-id="{{item.id}}">
		<span class="iconify" data-icon="clarity:edit-solid"></span>
	</button>
</div>
